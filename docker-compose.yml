services:
  app:
    build: .
    ports:
      - "4010:4010"
    volumes:
      # Persist the database outside the container
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - SQLITE_DB_PATH=/app/data/fhirtogether.db
    command: npm run dev
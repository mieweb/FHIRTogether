version: '3.8'

services:
  app:
    build: .
    ports:
      - "4010:4010"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - SQLITE_DB_PATH=/app/data/fhirtogether.db
    command: npm run dev